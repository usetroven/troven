import { ComingSoon } from "@/components/shared/ComingSoon";

export default function NotFound() {
  return (
    <ComingSoon
      headline={"This page is still\nbackstage."}
      subtitle="The link you followed isn’t live yet — or it wandered off the set. Try the home page, we’ve got plenty to show."
    />
  );
}
